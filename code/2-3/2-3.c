#include <stdio.h>
#include <string.h>

int main() {
    char a[64] = "Hello";
    char b[64] = "world";
    char c[128];

    size_t strlen(const char *a);
    printf("length of a: %d\n", len);
}
