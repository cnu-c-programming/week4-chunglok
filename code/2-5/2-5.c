#include <stdio.h>
#include <math.h>

int main() {
    double x = 9.0;
    double y = 2.5;

    prinf("%.2f\n", sqrt(x));
    prinf("%.2f\n", pow(x,y));
    prinf("%.1f\n", ceil(y));
    prinf("%.1f\n", floor(y));
    return 0;
}
